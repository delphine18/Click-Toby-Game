import React from "react";
import "./Header.css";

const Header = props => (
  <header className="header">
    <h1>Toby Game!</h1>
    <h2>
      Click on an image of a dog to earn points, but don't click on any more than once!
    </h2>
  </header>
);

export default Header;
